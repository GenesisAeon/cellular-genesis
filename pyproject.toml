[project]
name = "cellular-genesis"
version = "1.0.0"
description = "GenesisAeon Entropy Atlas — Packages 25 (Apoptosis) & 26 (Neuromorphic SNN)"
readme = "README.md"
license = { text = "MIT" }
authors = [{ name = "GenesisAeon" }]
requires-python = ">=3.11"
keywords = ["scaffold", "template", "cli", "project-generator", "skeleton"]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Code Generators",
]
dependencies = [
    "typer>=0.12.0",
    "rich>=13.7.0",
    "pyyaml>=6.0",
    "pydantic>=2.7",
    "diamond-setup>=2.2.0",
]

[project.optional-dependencies]
docs = [
    "mkdocs>=1.6.0",
    "mkdocs-material>=9.5.0",
]
dev = [
    "ruff>=0.6.0",
    "mypy>=1.10.0",
    "pre-commit>=3.7.0",
    "pytest>=8.0.0",
    "pytest-cov>=5.0.0",
]
cellular = [
    "numpy>=1.26.0",
    "scipy>=1.12.0",
]
spiking = [
    "numpy>=1.26.0",
]

[project.scripts]
diamond = "diamond_setup.cli:app"

[project.urls]
Homepage = "https://github.com/GenesisAeon/cellular-genesis"
Repository = "https://github.com/GenesisAeon/cellular-genesis"
Documentation = "https://genesisaeon.github.io/cellular-genesis"
Issues = "https://github.com/GenesisAeon/cellular-genesis/issues"
Zenodo = "https://doi.org/10.5281/zenodo.XXXXXXX"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cellular_genesis", "src/spiking_aeon"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "B", "I", "W", "UP", "C4", "SIM"]
ignore = ["B008"]  # typer uses function calls in defaults

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["S101"]  # allow assert in tests

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--cov=src --cov-report=term-missing -v"

[tool.coverage.run]
source = ["src"]
omit = ["tests/*"]

[tool.mypy]
python_version = "3.11"
strict = true
warn_return_any = true
warn_unused_configs = true
ignore_missing_imports = true

